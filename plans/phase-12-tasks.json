{
  "phase": 12,
  "name": "WebUI Redesign — Config-Driven Dashboard & Trend Charts",
  "tasks": [
    {
      "id": "12.0",
      "name": "Signal descriptor system",
      "description": "Create signal-descriptors.ts with SignalDescriptor/EquipmentGroup types, static unit/type lookup table from simulator PRD signal tables, defaultTrendSignals per equipment, and buildSignalDescriptors() function. Must handle unknown signals gracefully.",
      "prd_refs": ["§17"],
      "files": ["src/web/signal-descriptors.ts", "test/unit/web/signal-descriptors.test.ts"],
      "passes": true
    },
    {
      "id": "12.1",
      "name": "Dashboard page rewrite — equipment cards",
      "description": "Rewrite dashboard.tsx with config-driven equipment cards. One card per equipment group showing all signals: numeric values with units, boolean indicators, counter formatting, enum state badges. Dryer temp/setpoint pairing. Dynamic Datastar signals initialisation. Navigation links. Remove hardcoded 4-chart layout.",
      "prd_refs": ["§17"],
      "files": ["src/web/views/dashboard.tsx", "src/web/views/fragments/equipment-card.tsx", "src/web/views/fragments/signal-value.tsx", "test/unit/web/views/dashboard.test.ts"],
      "depends_on": ["12.0"],
      "passes": true
    },
    {
      "id": "12.2",
      "name": "SSE stream update — dynamic signal names",
      "description": "Verify and update flattenMetrics() so SSE signal names match the Datastar signal names used in the new dashboard. Update stream tests to use representative packaging profile metrics instead of old temperature/pressure/lineSpeed/humidity names.",
      "prd_refs": ["§17"],
      "files": ["src/web/routes/stream.ts", "test/unit/web/routes/stream.test.ts"],
      "depends_on": ["12.1"],
      "passes": true
    },
    {
      "id": "12.3",
      "name": "Trends page — hybrid curated + metric picker",
      "description": "Build /trends page with curated default charts per equipment group and 'Add metric' dropdown for remaining numeric signals. Time range selector (1h/8h/24h/1w). Reuse collatr-line-chart web component. Client-side metric picker with add/remove. Register route in server.ts.",
      "prd_refs": ["§17", "§11"],
      "files": ["src/web/views/trends.tsx", "src/web/public/components/metric-picker.js", "src/web/server.ts", "test/unit/web/views/trends.test.ts"],
      "depends_on": ["12.0"],
      "passes": true
    },
    {
      "id": "12.4",
      "name": "Staleness detection and visual indicators",
      "description": "Add per-signal staleness tracking: fresh (<30s), stale (30-60s), dead (>60s). Client-side JS module that observes signal changes and applies CSS classes. Amber/red visual feedback for stale/dead signals.",
      "prd_refs": ["§17"],
      "files": ["src/web/public/components/staleness.js", "src/web/views/fragments/signal-value.tsx", "test/unit/web/staleness.test.ts"],
      "depends_on": ["12.1"],
      "passes": true
    },
    {
      "id": "12.5",
      "name": "CSS refinements and responsive layout",
      "description": "Update CSS for equipment cards (full-width, signal grid), boolean indicators, counter formatting, enum badges, navigation bar, trends page layout, print-friendly styles.",
      "prd_refs": ["§17"],
      "files": ["src/web/views/layout.tsx"],
      "depends_on": ["12.1"],
      "passes": true
    },
    {
      "id": "12.6",
      "name": "Integration tests with factory simulator data",
      "description": "Update web-ui integration tests: verify dashboard HTML includes equipment cards with press/laminator/slitter signals, SSE stream sends signals matching config, /trends route works, /api/chart/metrics returns config metrics, backward compatibility for export/certificates/health.",
      "prd_refs": ["§17"],
      "files": ["test/integration/web-ui.test.ts", "test/integration/web-ui-trends.test.ts"],
      "depends_on": ["12.1", "12.3"],
      "passes": true
    },
    {
      "id": "12.7",
      "name": "Review fix: extract duplicate collectMetricNames (F-02)",
      "description": "Extract the duplicate collectMetricNames() function from dashboard.tsx and trends.tsx into a shared module (e.g. src/web/adapter-helpers.ts or export from signal-descriptors.ts). Both files must import from the shared location. Update tests if needed.",
      "prd_refs": ["§17"],
      "files": ["src/web/views/dashboard.tsx", "src/web/views/trends.tsx"],
      "depends_on": ["12.6"],
      "passes": true
    },
    {
      "id": "12.8",
      "name": "Review fix: staleness test imports actual module (F-04)",
      "description": "Update test/unit/web/staleness.test.ts to import classifyStaleness from the actual staleness.js module (which exports via module.exports) instead of re-implementing the logic. The test must use the real function to catch divergence if thresholds change.",
      "prd_refs": ["§17"],
      "files": ["test/unit/web/staleness.test.ts", "src/web/public/components/staleness.js"],
      "depends_on": ["12.6"],
      "passes": true
    },
    {
      "id": "12.9",
      "name": "Pipeline stats in status panel (agent.* metrics)",
      "description": "Wire pipeline operational counters (metricsGathered, metricsWritten, metricsDropped, gatherErrors, writeErrors) into the Pipeline Status panel on the dashboard. These come from the StatsCollector interface (src/core/stats.ts) which is already implemented. Add getStats() to the WebUIAdapter interface, wire it from PipelineRuntime, and render in the status panel alongside uptime/heap/RSS. Filter agent.* metrics OUT of the equipment cards — they are internal observability, not production data. Update status-panel.tsx SSE fragment to include the counters.",
      "prd_refs": ["§15", "§17"],
      "files": ["src/web/adapter.ts", "src/web/views/dashboard.tsx", "src/web/views/fragments/status-panel.tsx", "src/web/views/fragments/equipment-card.tsx"],
      "depends_on": ["12.7"],
      "passes": true
    }
  ]
}
